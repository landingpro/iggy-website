"use strict";(self.webpackChunkiggy_website=self.webpackChunkiggy_website||[]).push([[7012],{2920:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>l,contentTitle:()=>a,default:()=>h,frontMatter:()=>o,metadata:()=>s,toc:()=>c});const s=JSON.parse('{"id":"introduction/introduction-getting-started","title":"Getting started","description":"What we will build","source":"@site/docs/introduction/getting_started.md","sourceDirName":"introduction","slug":"/introduction/getting-started","permalink":"/docs/introduction/getting-started","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"id":"introduction-getting-started","slug":"/introduction/getting-started","title":"Getting started","sidebar_position":3},"sidebar":"tutorialSidebar","previous":{"title":"Architecture","permalink":"/docs/introduction/architecture"},"next":{"title":"High-level SDK","permalink":"/docs/introduction/high-level-sdk"}}');var i=t(4848),r=t(8453);const o={id:"introduction-getting-started",slug:"/introduction/getting-started",title:"Getting started",sidebar_position:3},a=void 0,l={},c=[{value:"What we will build",id:"what-we-will-build",level:2},{value:"Starting the Iggy server",id:"starting-the-iggy-server",level:2},{value:"Setting up the project",id:"setting-up-the-project",level:2},{value:"Building the producer",id:"building-the-producer",level:2},{value:"Building the consumer",id:"building-the-consumer",level:2},{value:"Summary",id:"summary",level:2}];function d(e){const n={a:"a",code:"code",h2:"h2",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.h2,{id:"what-we-will-build",children:"What we will build"}),"\n",(0,i.jsxs)(n.p,{children:["Once you're familiar with the ",(0,i.jsx)(n.a,{href:"/docs/introduction/architecture",children:"architecture"}),", it's high time to build the first application, or actually the set of applications - so-called ",(0,i.jsx)(n.strong,{children:"producer"})," (the one that will be publishing the messages to the stream) and ",(0,i.jsx)(n.strong,{children:"consumer"})," (yes, you got that right - it will handle the incoming messages stream)."]}),"\n",(0,i.jsx)(n.p,{children:"In the typical scenario, e.g. when working with the microservices architecture or any other kind of the distributed system, each application can be both at the same time (producer and consumer), as the particular service might publish its own messages (typically in a form of the events - facts, that have already happened) while at the same time be interested in the notifications coming from the other service(s)."}),"\n",(0,i.jsxs)(n.p,{children:["The completed sample can be found in ",(0,i.jsx)(n.a,{href:"https://github.com/apache/iggy/tree/master/core/examples/src/getting-started",children:"repository"}),", however, we will go through the implementation step by step, so that you can get a better understanding of how to build the applications from scratch."]}),"\n",(0,i.jsxs)(n.p,{children:["Also, please do keep in mind that we'll be using the default implementation of ",(0,i.jsx)(n.code,{children:"IggyClient"})," which provides a wrapper on top of the low-level, transport-specific ",(0,i.jsx)(n.code,{children:"Client"})," trait (which you can always use, if you want to have more control over the communication with the server)."]}),"\n",(0,i.jsxs)(n.p,{children:["On the other hand, if you're looking for a ",(0,i.jsx)(n.strong,{children:"more developer-friendly client builder API"})," providing additional features and extensions, please check the next article under ",(0,i.jsx)(n.a,{href:"/docs/introduction/high-level-sdk",children:"High-level SDK"})," section (this is typically how you'd use Iggy SDK in the real-world applications)."]}),"\n",(0,i.jsxs)(n.p,{children:["Nevertheless, let's start with the basic scenario, and build the producer and consumer applications using the default ",(0,i.jsx)(n.code,{children:"IggyClient"})," implementation, so you can get a better understanding of how to work with the streaming server."]}),"\n",(0,i.jsx)(n.h2,{id:"starting-the-iggy-server",children:"Starting the Iggy server"}),"\n",(0,i.jsxs)(n.p,{children:["For our purpose, we will focus on the basic scenario in order to keep things simple. Before we begin implementing the consumer and producer apps, we need to start the Iggy streaming server first - since there's no official package to be downloaded yet, the only way to get things up and running is by cloning the ",(0,i.jsx)(n.a,{href:"https://github.com/apache/iggy",children:"repository"})," and then starting the server by running the following command: ",(0,i.jsx)(n.code,{children:"cargo r --bin iggy-server"})," or by building & running the Docker image via ",(0,i.jsx)(n.code,{children:"docker compose up"}),". The official images can be found ",(0,i.jsx)(n.a,{href:"https://hub.docker.com/r/apache/iggy",children:"here"}),", simply type ",(0,i.jsx)(n.code,{children:"docker pull apache/iggy"}),". All the data used by the server, will be persisted under ",(0,i.jsx)(n.code,{children:"local_data"})," directory, unless specified differently in the configuration."]}),"\n",(0,i.jsxs)(n.p,{children:["As long as the Iggy server is running, we're good to go. If you'd like to play with the configuration (e.g. change the addresses, ports, caching or so) you will find the ",(0,i.jsx)(n.code,{children:"server.toml"})," under ",(0,i.jsx)(n.code,{children:"configs"})," directory in the root of the repository."]}),"\n",(0,i.jsx)(n.h2,{id:"setting-up-the-project",children:"Setting up the project"}),"\n",(0,i.jsxs)(n.p,{children:["Let's start by creating a new project - simply type ",(0,i.jsx)(n.code,{children:"cargo new iggy-sample"})," and open your newly created project with the favorite code editor."]}),"\n",(0,i.jsxs)(n.p,{children:["At the very beginning, we will organize the files a little bit differently - create a new ",(0,i.jsx)(n.code,{children:"consumer"})," directory under ",(0,i.jsx)(n.code,{children:"src"})," and move the ",(0,i.jsx)(n.code,{children:"main.rs"})," file here. You should have the following project structure:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"\u251c\u2500\u2500 Cargo.lock\n\u251c\u2500\u2500 Cargo.toml\n\u251c\u2500\u2500 src\n    \u2514\u2500\u2500 consumer\n        \u2514\u2500\u2500 main.rs\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Then, open ",(0,i.jsx)(n.code,{children:"Cargo.toml"})," and add the following code before the ",(0,i.jsx)(n.code,{children:"[dependencies]"})]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:'[[bin]]\nname = "consumer"\npath = "src/consumer/main.rs"\n'})}),"\n",(0,i.jsxs)(n.p,{children:["Just to make sure that everything is set correctly, try to run the app with ",(0,i.jsx)(n.code,{children:"cargo r --bin consumer"}),"."]}),"\n",(0,i.jsxs)(n.p,{children:["Now, let's do the same for the ",(0,i.jsx)(n.code,{children:"producer"})," part. Create a new ",(0,i.jsx)(n.code,{children:"producer"})," directory, copy & paste the existing ",(0,i.jsx)(n.code,{children:"main.rs"}),", and update the ",(0,i.jsx)(n.code,{children:"Cargo.toml"})," with the following code:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:'[[bin]]\nname = "producer"\npath = "src/producer/main.rs"\n'})}),"\n",(0,i.jsx)(n.p,{children:"You should have the project structure as shown below:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"\u251c\u2500\u2500 Cargo.lock\n\u251c\u2500\u2500 Cargo.toml\n\u251c\u2500\u2500 src\n    \u251c\u2500\u2500 consumer\n    \u2502   \u2514\u2500\u2500 main.rs\n    \u2514\u2500\u2500 producer\n        \u2514\u2500\u2500 main.rs\n"})}),"\n",(0,i.jsx)(n.p,{children:"From that point on, we will focus on implementing the message streaming between our applications, starting with the producer."}),"\n",(0,i.jsx)(n.h2,{id:"building-the-producer",children:"Building the producer"}),"\n",(0,i.jsxs)(n.p,{children:["We will begin with installing the Iggy client crate - execute ",(0,i.jsx)(n.code,{children:"cargo add iggy"})," in your terminal. Next, install ",(0,i.jsx)(n.a,{href:"https://tokio.rs",children:"tokio.rs"})," dependency with ",(0,i.jsx)(n.code,{children:"cargo add tokio"})," as we will use the asynchronous runtime. Eventually, modify your ",(0,i.jsx)(n.code,{children:"main.rs"}),", so it looks like this:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:"use std::error::Error;\n\n#[tokio::main]\nasync fn main() -> Result<(), Box<dyn Error>> {\n    Ok(())\n}\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Let's instantiate a new connection to the server. As the description states, Iggy does support different transport protocols such as TCP, QUIC and HTTP, which is entirely true. Yet remember, we want to keep things as simple as possible here, thus, we will make use of the default implementation of ",(0,i.jsx)(n.code,{children:"IggyClient"})," struct, which wraps the underlying raw client for one of the selected transports (in our scenario, this will be ",(0,i.jsx)(n.code,{children:"TcpClient"}),")."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:"let client = IggyClient::default();\nclient.connect().await?;\n"})}),"\n",(0,i.jsxs)(n.p,{children:["The default server address being ",(0,i.jsx)(n.code,{children:"127.0.0.1:8090"})," is configured on the server side, and can be easily adjusted by updating the ",(0,i.jsx)(n.code,{children:"server.toml"})," (the default configuration) or ",(0,i.jsx)(n.code,{children:"server.json"})," file in ",(0,i.jsx)(n.code,{children:"configs"})," directory."]}),"\n",(0,i.jsxs)(n.p,{children:["We could make use of more advanced components such as ",(0,i.jsx)(n.a,{href:"https://github.com/apache/iggy/blob/master/core/sdk/src/client_provider.rs",children:(0,i.jsx)(n.code,{children:"ClientProvider"})}),", pass the custom configuration built via console args to choose between the different protocols as all the available clients implement the same ",(0,i.jsx)(n.a,{href:"https://github.com/apache/iggy/blob/master/core/sdk/src/clients/client.rs",children:"Client"})," trait and so on."]}),"\n",(0,i.jsxs)(n.p,{children:["If you're eager to find out how to build more advanced (and configurable) applications, check the Rust ",(0,i.jsx)(n.a,{href:"/docs/sdk/rust/examples",children:"examples"}),". Nevertheless, let's focus on implementing our producer side :)"]}),"\n",(0,i.jsxs)(n.p,{children:["Next, we need to authenticate the user, as all the available actions (except the ",(0,i.jsx)(n.code,{children:"ping"})," and ",(0,i.jsx)(n.code,{children:"login"}),") require the user to be authenticated and have the appropriate permissions e.g. you might be able to send or poll the messages, but you won't be able to create the stream or topic. For the sake of simplicity, we will use the default credentials for the root user (username: ",(0,i.jsx)(n.code,{children:"iggy"}),", password: ",(0,i.jsx)(n.code,{children:"iggy"}),"), that can do anything and cannot be deleted. You can easily create more users and assign the specific permissions to them, however, this is out of scope for this tutorial."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:"client\n    .login_user(DEFAULT_ROOT_USERNAME, DEFAULT_ROOT_PASSWORD)\n    .await?;\n"})}),"\n",(0,i.jsxs)(n.p,{children:["When you start the application now, by running ",(0,i.jsx)(n.code,{children:"cargo r --bin producer"})," it will execute immediately, however, you should be able to see the logs on Iggy server - connection should be accepted, user logged in, and then connection should be closed right away, meaning that we've just established our very first communication with the streaming server."]}),"\n",(0,i.jsxs)(n.p,{children:["Before we will move on with the code, let's include some logging in our apps as well (Iggy client already has some logging in place on different levels). Let's make use of the ",(0,i.jsx)(n.a,{href:"https://tracing.rs",children:"tracing.rs"})," crates: ",(0,i.jsx)(n.code,{children:"cargo add tracing tracing_subscriber"}),"."]}),"\n",(0,i.jsxs)(n.p,{children:["To make use of logging, simply invoke ",(0,i.jsx)(n.code,{children:"tracing_subscriber::fmt::init()"})," at the beginning of ",(0,i.jsx)(n.code,{children:"main()"})," method:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:"use std::error::Error;\nuse iggy::client::{Client, UserClient};\nuse iggy::clients::client::IggyClient;\nuse iggy::users::defaults::{DEFAULT_ROOT_PASSWORD, DEFAULT_ROOT_USERNAME};\n\n#[tokio::main]\nasync fn main() -> Result<(), Box<dyn Error>> {\n    tracing_subscriber::fmt::init();\n    let client = IggyClient::default();\n    client.connect().await?;\n    client\n        .login_user(DEFAULT_ROOT_USERNAME, DEFAULT_ROOT_PASSWORD)\n        .await?;\n    Ok(())\n}\n"})}),"\n",(0,i.jsxs)(n.p,{children:["From that point on, when starting the application, you should be able to see at least the basic logs, for example regarding the connection status. Keep in mind, that you can change the default log level by setting ",(0,i.jsx)(n.code,{children:"RUST_LOG"})," environment variable, e.g. ",(0,i.jsx)(n.code,{children:"export RUST_LOG=trace"})," (or other command depending on OS)."]}),"\n",(0,i.jsxs)(n.p,{children:["So far, so good, however, before we will be able to publish any messages to our streaming server, at first, we need to create the stream, topic and partition(s) - if you're unfamiliar with these concepts, please refer to ",(0,i.jsx)(n.a,{href:"/docs/introduction/architecture",children:"architecture"})," where all these concepts are described in-depth."]}),"\n",(0,i.jsxs)(n.p,{children:["Since our ",(0,i.jsx)(n.code,{children:"IggyClient"})," implements the common ",(0,i.jsx)(n.a,{href:"https://github.com/apache/iggy/blob/master/core/sdk/src/clients/client.rs",children:"Client"})," trait, you can find lots of the different methods to interact with the server, also from the administrative point of view, e.g. creating the streams, topics etc."]}),"\n",(0,i.jsx)(n.p,{children:"One thing worth mentioning is that, these methods are not idempotent - for example, if you were to try creating the stream with the same ID which already exists on the server, you would receive the specific error. In such a case, you can simply check for an error and move on. Let's do this then :)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:'use iggy::client::{Client, StreamClient, TopicClient, UserClient};\nuse iggy::clients::client::IggyClient;\nuse iggy::identifier::Identifier;\nuse iggy::streams::create_stream::CreateStream;\nuse iggy::topics::create_topic::CreateTopic;\nuse std::error::Error;\nuse tracing::{info, warn};\nuse iggy::compression::compression_algorithm::CompressionAlgorithm;\nuse iggy::users::defaults::{DEFAULT_ROOT_PASSWORD, DEFAULT_ROOT_USERNAME};\nuse iggy::utils::expiry::IggyExpiry;\nuse iggy::utils::topic_size::MaxTopicSize;\n\nconst STREAM_ID: u32 = 1;\nconst TOPIC_ID: u32 = 1;\n\n#[tokio::main]\nasync fn main() -> Result<(), Box<dyn Error>> {\n    tracing_subscriber::fmt::init();\n    let client = IggyClient::default();\n    client.connect().await?;\n    client\n        .login_user(DEFAULT_ROOT_USERNAME, DEFAULT_ROOT_PASSWORD)\n        .await?;\n    init_system(&client).await;\n    Ok(())\n}\n\nasync fn init_system(client: &IggyClient) {\n    match client.create_stream("sample-stream", Some(STREAM_ID)).await {\n        Ok(_) => info!("Stream was created."),\n        Err(_) => warn!("Stream already exists and will not be created again."),\n    }\n\n    match client\n        .create_topic(\n            &STREAM_ID.try_into().unwrap(),\n            "sample-topic",\n            1,\n            CompressionAlgorithm::default(),\n            None,\n            Some(TOPIC_ID),\n            IggyExpiry::NeverExpire,\n            MaxTopicSize::ServerDefault,\n        )\n        .await\n    {\n        Ok(_) => info!("Topic was created."),\n        Err(_) => warn!("Topic already exists and will not be created again."),\n    }\n}\n'})}),"\n",(0,i.jsx)(n.p,{children:"Finally, let's send some messages into our stream. We will implement the basic loop with an interval between each iteration to simulate publishing the batch of messages. Since the streaming server works directly with the binary data, and couldn't care less about the (de)serialization format for the message payload, it's really up to you, how to efficiently stream the messages for your use case."}),"\n",(0,i.jsxs)(n.p,{children:["In our example, we will simply use the string payload, and pass it via ",(0,i.jsx)(n.code,{children:"from_str"})," trait to construct the ",(0,i.jsx)(n.a,{href:"https://github.com/apache/iggy/blob/master/core/common/src/commands/messages/send_messages.rs#L423",children:"Message"}),"."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:'let payload = "hello world";\nlet message = Message::from_str(&payload)?;\n'})}),"\n",(0,i.jsx)(n.p,{children:"Next, we need to call the appropriate method to send the messages to the server:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:'let payload = "hello world";\nlet message = Message::from_str(&payload)?;\nlet messages = vec![message];\nlet partitioning = Partitioning::partition_id(PARTITION_ID);\n\nclient\n    .send_messages(\n        &STREAM_ID.try_into().unwrap(),\n        &TOPIC_ID.try_into().unwrap(),\n        &partitioning,\n        &mut messages,\n    )\n    .await?;\n'})}),"\n",(0,i.jsxs)(n.p,{children:["As you might've already noticed, there are quite a few fields to be assigned. While some of them should be rather self-descriptive, let's discuss the ",(0,i.jsx)(n.code,{children:"identifier"})," (created implicitly by using ",(0,i.jsx)(n.code,{children:"TryFrom"})," on top of ",(0,i.jsx)(n.code,{children:"STREAM_ID"})," and ",(0,i.jsx)(n.code,{children:"TOPIC_ID"}),") and the ",(0,i.jsx)(n.code,{children:"partitioning"})," which might be a little bit confusing at first, starting with the former."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:"pub struct Identifier {\n    pub kind: IdKind,\n    pub length: u8,\n    pub value: Vec<u8>,\n}\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Whenever we interact with the streaming server in terms of e.g. sending or polling the messages, managing the streams, topics etc. we need to provide the unique identifier of the stream and the topic that we want to use. Since each stream and topic have a unique numeric ID, as well as a unique name, we can use either of them. Here, we use the numeric ID by calling ",(0,i.jsx)(n.code,{children:"Identifier::numeric()"}),", however, you can also use the string identifier by invoking the ",(0,i.jsx)(n.code,{children:"Identifier::named()"})," method instead. It's up to your preference if you'd rather work with the identifier being a number or string when building your applications."]}),"\n",(0,i.jsxs)(n.p,{children:["Next, let's move onto the ",(0,i.jsx)(n.code,{children:"partitioning"})," field:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:"pub struct Partitioning {\n    pub kind: PartitioningKind,\n    pub length: u8,\n    pub value: Vec<u8>,\n}\n"})}),"\n",(0,i.jsxs)(n.p,{children:["In our scenario, we simply make use of ",(0,i.jsx)(n.code,{children:"PartitioningKind::PartitionId"})," (by invoking the helper method ",(0,i.jsx)(n.code,{children:"partition_id()"}),") meaning that we want to send the messages to the specified partition ID - in that case, the length will be 4 bytes (as the partition ID is of type ",(0,i.jsx)(n.code,{children:"u32"}),"), and the value will be the actual partition ID encoded as the bytes array."]}),"\n",(0,i.jsxs)(n.p,{children:["However, once your system grows, you might want to parallelize the messages across the independent consumers, in order to achieve the horizontal scaling, higher resiliency etc. In that case, you might consider using either ",(0,i.jsx)(n.code,{children:"PartitioningKind::Balanced"})," (this will automatically calculate the next partition ID using the simple round-robin algorithm approach e.g. 1->2->3->1->2->3 etc.) or ",(0,i.jsx)(n.code,{children:"PartitioningKind::MessagesKey"})," instead (e.g. by invoking one of the helper methods ",(0,i.jsx)(n.code,{children:"messages_key()"}),"), where the value wouldn't be a partition ID anymore (this would be calculated on the server side using murmur3 hash of the received value), but, as the name states, some kind of identifier, which is unique for all the messages that should have guaranteed ordering."]}),"\n",(0,i.jsx)(n.p,{children:"For example, given that you process the set of messages related to the specific order ID (e.g. created, confirmed, paid, delivered etc.), you could use that as a key value to ensure that all the messages which are part of the specific workflow, will always be put onto the same partition. The value of the key can be anything (e.g. string, number) with the maximum length of 255 bytes."}),"\n",(0,i.jsx)(n.p,{children:"Anyway, let's get back to our scenario, and consider the following code responsible for publishing the messages:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:'async fn produce_messages(client: &IggyClient) -> Result<(), Box<dyn Error>> {\n    let interval = Duration::from_millis(500);\n    info!(\n        "Messages will be sent to stream: {}, topic: {}, partition: {} with interval {} ms.",\n        STREAM_ID,\n        TOPIC_ID,\n        PARTITION_ID,\n        interval.as_millis()\n    );\n\n    let mut current_id = 0;\n    let messages_per_batch = 10;\n    let partitioning = Partitioning::partition_id(PARTITION_ID);\n    loop {\n        let mut messages = Vec::new();\n        for _ in 0..messages_per_batch {\n            current_id += 1;\n            let payload = format!("message-{current_id}");\n            let message = Message::from_str(&payload)?;\n            messages.push(message);\n        }\n        client\n            .send_messages(\n                &STREAM_ID.try_into().unwrap(),\n                &TOPIC_ID.try_into().unwrap(),\n                &partitioning,\n                &mut messages,\n            )\n            .await?;\n        info!("Sent {messages_per_batch} message(s).");\n        sleep(interval).await;\n    }\n}\n'})}),"\n",(0,i.jsxs)(n.p,{children:["The reason behind passing a mutable reference is that the underlying client (especially when using the ",(0,i.jsx)(n.code,{children:"IggyClient"})," wrapper on top of the low-level client) might want to modify the command before sending it to the server. For example, the client might want to encrypt the payload, include the default headers, or provide a custom ",(0,i.jsx)(n.code,{children:"Partitioner"})," implementation etc. thus by using ",(0,i.jsx)(n.code,{children:"&mut"})," we can avoid copying the command each time."]}),"\n",(0,i.jsx)(n.p,{children:"Finally, let's complete the implementation of the producer - once you start the application after the latest changes, you shall see the messages being sent to the newly created stream."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:'use iggy::client::{Client, MessageClient, StreamClient, TopicClient, UserClient};\nuse iggy::clients::client::IggyClient;\nuse iggy::messages::send_messages::{Message, Partitioning};\nuse std::error::Error;\nuse std::str::FromStr;\nuse std::time::Duration;\nuse tokio::time::sleep;\nuse tracing::{info, warn};\nuse iggy::compression::compression_algorithm::CompressionAlgorithm;\nuse iggy::users::defaults::{DEFAULT_ROOT_PASSWORD, DEFAULT_ROOT_USERNAME};\nuse iggy::utils::expiry::IggyExpiry;\nuse iggy::utils::topic_size::MaxTopicSize;\n\nconst STREAM_ID: u32 = 1;\nconst TOPIC_ID: u32 = 1;\nconst PARTITION_ID: u32 = 1;\n\n#[tokio::main]\nasync fn main() -> Result<(), Box<dyn Error>> {\n    tracing_subscriber::fmt::init();\n    let client = IggyClient::default();\n    client.connect().await?;\n    client\n        .login_user(DEFAULT_ROOT_USERNAME, DEFAULT_ROOT_PASSWORD)\n        .await?;\n    init_system(&client).await;\n    produce_messages(&client).await?;\n    Ok(())\n}\n\nasync fn init_system(client: &IggyClient) {\n    match client.create_stream("sample-stream", Some(STREAM_ID)).await {\n        Ok(_) => info!("Stream was created."),\n        Err(_) => warn!("Stream already exists and will not be created again."),\n    }\n\n    match client\n        .create_topic(\n            &STREAM_ID.try_into().unwrap(),\n            "sample-topic",\n            1,\n            CompressionAlgorithm::default(),\n            None,\n            Some(TOPIC_ID),\n            IggyExpiry::NeverExpire,\n            MaxTopicSize::ServerDefault,\n        )\n        .await\n    {\n        Ok(_) => info!("Topic was created."),\n        Err(_) => warn!("Topic already exists and will not be created again."),\n    }\n}\n\nasync fn produce_messages(client: &IggyClient) -> Result<(), Box<dyn Error>> {\n    let interval = Duration::from_millis(500);\n    info!(\n        "Messages will be sent to stream: {}, topic: {}, partition: {} with interval {} ms.",\n        STREAM_ID,\n        TOPIC_ID,\n        PARTITION_ID,\n        interval.as_millis()\n    );\n\n    let mut current_id = 0;\n    let messages_per_batch = 10;\n    let partitioning = Partitioning::partition_id(PARTITION_ID);\n    loop {\n        let mut messages = Vec::new();\n        for _ in 0..messages_per_batch {\n            current_id += 1;\n            let payload = format!("message-{current_id}");\n            let message = Message::from_str(&payload)?;\n            messages.push(message);\n        }\n        client\n            .send_messages(\n                &STREAM_ID.try_into().unwrap(),\n                &TOPIC_ID.try_into().unwrap(),\n                &partitioning,\n                &mut messages,\n            )\n            .await?;\n        info!("Sent {messages_per_batch} message(s).");\n        sleep(interval).await;\n    }\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"building-the-consumer",children:"Building the consumer"}),"\n",(0,i.jsxs)(n.p,{children:["The consumer part will be rather easy to implement. Just like on the producer side, we want to make use of the asynchronous runtime, logging and last but not least, the same ",(0,i.jsx)(n.code,{children:"Client"})," trait. Let's find out, how we can make use of the ",(0,i.jsx)(n.code,{children:"poll_messages"})," method, to fetch the messages from the stream. Consider the following code:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:"let polled_messages = client\n    .poll_messages(\n        &STREAM_ID.try_into()?,\n        &TOPIC_ID.try_into()?,\n        Some(PARTITION_ID),\n        &Consumer::default(),\n        &PollingStrategy::offset(offset),\n        messages_per_batch,\n        false,\n    )\n    .await?;\n"})}),"\n",(0,i.jsxs)(n.p,{children:["At the first glance, it might look a bit more complicated than ",(0,i.jsx)(n.code,{children:"send_messages"})," function, so let's discuss the fields one by one:"]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"stream_id"})," - the ID of the stream (numeric or string) from which we want to poll the messages."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"topic_id"})," - the ID of the topic (numeric or string) from which we want to poll the messages."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"partition_id"})," - the ID of the partition from which we want to poll the messages. The partition has to be specified for the regular ",(0,i.jsx)(n.code,{children:"Consumer"}),", while for the ",(0,i.jsx)(n.code,{children:"ConsumerGroup"})," it's ignored (",(0,i.jsx)(n.code,{children:"None"})," value), as the server will automatically assign the partition to the consumer from the group."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"consumer"})," - the type of the consumer (kind + ID), either the default ",(0,i.jsx)(n.code,{children:"Consumer"})," means the standalone client which does the message polling on its own, independently of the other consumers (unless they would use the same ID), or the ",(0,i.jsx)(n.code,{children:"ConsumerGroup"})," which might be used to create the group of consumers sharing the common identifier - this is especially useful in the case of the horizontal scaling, where we want to ensure, that the same (and only one) consumer, will poll the messages from the specific partition, and there will be no overlap with the other consumers from the same group. For example, when scaling out (by adding more instances) the group of payment processing microservices, we probably don't want the multiple instances to process the same payment."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"strategy"})," - the way in which we want to poll the messages. The default one being ",(0,i.jsx)(n.code,{children:"Offset"})," (underlying ",(0,i.jsx)(n.code,{children:"PollingKind"})," enum) means that we will start polling the messages from the particular offset provided in the ",(0,i.jsx)(n.code,{children:"value"})," field - it's on the client, to keep track of the most recent offset. On the other hand, we could also use the different kind, for example ",(0,i.jsx)(n.code,{children:"Next"}),", which means, that the next messages will be returned to the client, depending on the so-called ",(0,i.jsx)(n.code,{children:"consumer offset"})," value stored on the server side. The client may not need to track the offset on its own anymore, but instead, call ",(0,i.jsx)(n.code,{children:"store_offset()"})," to save it on the server (e.g. after each processed message or the whole batch), or make use of  ",(0,i.jsx)(n.code,{children:"auto_commit: true"}),", to automatically commit the offset on the server side, once the messages are fetched (this one results in the so called at-most-once delivery mode)."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"count"})," - amount of the messages that the consumer would like to receive in the single response from the server."]}),"\n"]}),"\n",(0,i.jsxs)(n.li,{children:["\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"auto_commit"})," - whether the consumer offset should be automatically committed on the server side, once the messages are fetched."]}),"\n"]}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:"Next, let's take a look at the following method responsible for polling the messages based on the specified interval."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:'async fn consume_messages(client: &IggyClient) -> Result<(), Box<dyn Error>> {\n    let interval = Duration::from_millis(500);\n    let mut offset = 0;\n    let messages_per_batch = 10;\n    let consumer = Consumer::default();\n    let mut strategy = PollingStrategy::offset(offset);\n    loop {\n        let polled_messages = client\n            .poll_messages(\n                &STREAM_ID.try_into()?,\n                &TOPIC_ID.try_into()?,\n                Some(PARTITION_ID),\n                &consumer,\n                &PollingStrategy::offset(offset),\n                messages_per_batch,\n                false,\n            )\n            .await?;\n\n        if polled_messages.messages.is_empty() {\n            info!("No messages found.");\n            sleep(interval).await;\n            continue;\n        }\n\n        offset += polled_messages.messages.len() as u64;\n        strategy.set_value(offset);\n        for message in polled_messages.messages {\n            handle_message(&message)?;\n        }\n        sleep(interval).await;\n    }\n}\n'})}),"\n",(0,i.jsx)(n.p,{children:"And here's the final code for our consumer application:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-rust",children:'use iggy::client::{Client, MessageClient, UserClient};\nuse iggy::clients::client::IggyClient;\nuse iggy::consumer::Consumer;\nuse iggy::messages::poll_messages::PollingStrategy;\nuse iggy::models::messages::PolledMessage;\nuse std::error::Error;\nuse std::time::Duration;\nuse tokio::time::sleep;\nuse tracing::info;\nuse iggy::users::defaults::{DEFAULT_ROOT_PASSWORD, DEFAULT_ROOT_USERNAME};\n\nconst STREAM_ID: u32 = 1;\nconst TOPIC_ID: u32 = 1;\nconst PARTITION_ID: u32 = 1;\n\n#[tokio::main]\nasync fn main() -> Result<(), Box<dyn Error>> {\n    tracing_subscriber::fmt::init();\n    let client = IggyClient::default();\n    client.connect().await?;\n    client\n        .login_user(DEFAULT_ROOT_USERNAME, DEFAULT_ROOT_PASSWORD)\n        .await?;\n    consume_messages(&client).await\n}\n\nasync fn consume_messages(client: &IggyClient) -> Result<(), Box<dyn Error>> {\n    let interval = Duration::from_millis(500);\n    info!(\n        "Messages will be consumed from stream: {}, topic: {}, partition: {} with interval {} ms.",\n        STREAM_ID,\n        TOPIC_ID,\n        PARTITION_ID,\n        interval.as_millis()\n    );\n\n    let mut offset = 0;\n    let messages_per_batch = 10;\n    let consumer = Consumer::default();\n    let mut strategy = PollingStrategy::offset(offset);\n    loop {\n        let polled_messages = client\n            .poll_messages(\n                &STREAM_ID.try_into()?,\n                &TOPIC_ID.try_into()?,\n                Some(PARTITION_ID),\n                &consumer,\n                &PollingStrategy::offset(offset),\n                messages_per_batch,\n                false,\n            )\n            .await?;\n\n        if polled_messages.messages.is_empty() {\n            info!("No messages found.");\n            sleep(interval).await;\n            continue;\n        }\n\n        offset += polled_messages.messages.len() as u64;\n        strategy.set_value(offset);\n        for message in polled_messages.messages {\n            handle_message(&message)?;\n        }\n        sleep(interval).await;\n    }\n}\n\nfn handle_message(message: &PolledMessage) -> Result<(), Box<dyn Error>> {\n    // The payload can be of any type as it is a raw byte array. In this case it\'s a simple string.\n    let payload = std::str::from_utf8(&message.payload)?;\n    info!(\n        "Handling message at offset: {}, payload: {}...",\n        message.offset, payload\n    );\n    Ok(())\n}\n'})}),"\n",(0,i.jsx)(n.p,{children:"Start the Iggy server, and then producer and consumer applications respectively - you should observe your messages being streamed flawlessly :)"}),"\n",(0,i.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,i.jsxs)(n.p,{children:["What we've just achieved is merely the tip of an iceberg, however, it should give you a good understanding of what Iggy is all about, and hopefully, it wasn't too difficult to follow and get things up and running for the first time. Feel free to take a look at the more advanced ",(0,i.jsx)(n.a,{href:"https://github.com/apache/iggy/tree/master/core/examples",children:"examples"}),", how to make use of ",(0,i.jsx)(n.code,{children:"IggyClient"})," wrapper on top of the existing implementations of the ",(0,i.jsx)(n.code,{children:"Client"})," trait, what happens when you start multiple producers and consumers and so on. Happy tweaking! :)"]})]})}function h(e={}){const{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}},8453:(e,n,t)=>{t.d(n,{R:()=>o,x:()=>a});var s=t(6540);const i={},r=s.createContext(i);function o(e){const n=s.useContext(r);return s.useMemo((function(){return"function"==typeof e?e(n):{...n,...e}}),[n,e])}function a(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:o(e.components),s.createElement(r.Provider,{value:n},e.children)}}}]);